function Marble(pos, color)
{
	this.pos = pos;
  this.color = color;
};

function Pit(pos, color)
{
	this.pos = pos;
  this.color = color;
};

function Line(start, end, color)
{
	this.start = start;
	this.end = end;
  this.color = color;
};

function Player(name, color)
{
	this.name = name;
  this.color = color;
};